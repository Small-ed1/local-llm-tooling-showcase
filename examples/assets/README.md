# Example Assets

Small demo inputs for optional tools live here instead of the repository root.

- `sample.pdf` is used by `parse_pdf` examples.
- `speech.wav` is used by `transcribe_audio` examples.

Generated outputs, including TTS audio, should stay untracked.
